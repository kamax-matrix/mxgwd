matrix:
  client:
    hosts:
      synapse.local.kamax.io:
        to: 'http://localhost:8008'
        endpoints:
        - path: '/_matrix/client/r0/profile/'
          acls:
          - type: 'whitelist'
            target: 'method'
            value: 'GET'
